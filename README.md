# gha-demo

A tiny repository to try a GitHub Actions demo workflow.

## Demo workflow

This repository includes `.github/workflows/demo.yml`.

It runs when:
- you push a commit
- you manually trigger it from **Actions → GitHub Actions Demo → Run workflow**
